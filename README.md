To prepare:
- Run in cmd the following cli pip install -r requirements.txt
- Create a folder "data" and run the script collect.py under data_collection folder to capture the frames that will be used as dataset
- Add Specific sound effects under the folder "sound"
- Add images to be displayed with each gesture under the folder "filter"
- Use any open source code to generate a tensorflow.h5 model that will be used for the prediciton in test.py
- Save the model and add it under data folder and udpate labels.txt accordingly
- Run the script test.py
